name: Deployment Exercise 1
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Run linting
        run: npm run lint
      - name: Run test
        run: npm run test
      - name: Build code
        run: npm Build
      - name: Deployment
        run: echo "Sample Deploy message. Replace with a real deployment workflow"